@model Landlyst.Models.Rooms

<div>
    <table>
        <tr>
            <td style="margin:20px; display:inline-block">
                <div>
                    <label>
                        Altan <br />
                        <input type="checkbox" class="search" onchange="test()" value="Balcony," />
                    </label>
                </div>
            </td>

            <td style="margin:20px; display:inline-block">
                <div>
                    <label>
                        2 senge / dobbelt seng <br />
                        <input type="checkbox" class="search" onchange="test()" value="Multiple beds," />
                    </label>
                </div>
            </td>

            <td style="margin:20px; display:inline-block">
                <div>
                    <input value="Bathtub" name="temp" hidden />
                    <label>
                        Badekar <br />
                        <input type="checkbox" class="search" onchange="test()" value="Bathtub," />
                    </label>
                </div>
            </td>

            <td style="margin:20px; display:inline-block">
                <div>
                    <label>
                        Jacuzzi <br />
                        <input type="checkbox" class="search" onchange="test()" value="Jacuzzi," />
                    </label>
                </div>
            </td>

            <td style="margin:20px; display:inline-block">
                <div>
                    <label>
                        Køkken <br />
                        <input type="checkbox" class="search" onchange="test()" value="Kitchen," />
                    </label>
                </div>
            </td>
            <td>
                <div>
                    @using (Html.BeginForm("Booking", "Home"))
                    {
                        <button type="submit" id="res" name="temp">Søg</button>
                    }
                </div>
            </td>
        </tr>
    </table>
</div>

<table>
    @for (int i = 0; i < Model.rooms.Count; i++)
    {
        if (i == 0 || i % 3 == 0)
        {
            <tr></tr>
        }
        <td style="display:inline-block; margin-right:20px; margin-bottom:20px">
            <div style="border:solid;">
                <p>
                    rum nummer @Model.rooms[i].Number
                    <br />
                    starts pris @Model.rooms[i].Price
                    <br />
                    Tilbehør:
                    <br />
                    @foreach (string utility in Model.rooms[i].GetUtilities())
                    {
                        @utility
                        <br />
                    }
                </p>
                <input type="checkbox" id="@Model.rooms[i].Number" />
            </div>
        </td>
    }
</table>

<script>
    function test() {
        var res = "";
        $(".search:checked").each(function (i, n) { res += $(n).val(); })
        $("#res").val(res);
    }
</script>